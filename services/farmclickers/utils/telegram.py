from utils.logger import logger

async def create_session():
    print("Session creation is no longer needed.")
    print("Add your WebApp init_data tokens to data.txt (one per line).")

async def leave_chats(session_name):
    logger.warning("leave_chats is no longer available (pyrogram sessions removed)")
